node_modules/
*.log
.DS_Store

# Local personal config. v2 reads devices live from Windows and ignores this file, but keep any
# stray config.json (it can hold real device names) out of git.
com.welsh.audioswap.sdPlugin/config.json

# Dev-only artifacts: generated previews + machine-specific integration harness.
icon-preview.png
pi-preview.png
tools/test-harness.js
